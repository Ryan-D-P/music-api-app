import { useParams, Link } from "react-router-dom";
import useFetchWorks from "../hooks/useFetchComposers";

const Works = () => {
    return (
        <div className="Works">

        </div>
    );
}

export default Works;